public int[] frontPiece(int[] nums) {
  /*
  Return an array with the first 2 elements from the input nums
  or return nums
  */
  
  if (nums.length < 2){
    return nums;
  }
  
  return new int[]{nums[0], nums[1]};
}

/*
Solution from function name:
  Return frontPiece
  
Solution from description:
  return a new array of its first 2 elements
  
Given:
  an int array nums of any length
  
Constraints:
  if the array is smaller than length 2, use whatever elements are present
  
Intermediate translations:
  a new array of its first 2 elments -> copy first two elements from nums to new array
  use whatever elements are present -> return nums

*/
