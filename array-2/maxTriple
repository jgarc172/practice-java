public int maxTriple(int[] nums) {
  /*
    Return the largest, max, of first, middle, and last elements
  */
  
  int middle = nums.length / 2;
  int last = nums.length - 1;
  int max = Math.max(nums[0], nums[middle]);
  max = Math.max(max, nums[last]);
  
  return max;
}

/*
Solution from description:
  return the largest from first, middle, and last
  
Given and constraints:
  an array of ints nums of odd length and length of at least 1

Intermediate translations:
  first index -> 0
  last index -> length - 1
  middle index -> length / 2
  

*/
