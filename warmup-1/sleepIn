/*
  solution from function name:
    sleepIn is true or false (boolean)
    
  solution from description:  
    return true if we sleep in
    We sleep in if it is not a weekday or we're on vacation
    
  Given:
    The parameter weekday is true if it is a weekday, and
    the parameter vacation is true if we are on vacation
    
  Intermediate translations:
    solution: 
      return true if it is not a weekday or we're on vacation
    
    no weekday -> !weekday
    or -> ||
    on vacation -> vacation
    
  solution: 
   return true if (not weekday or on vacation)
*/
public boolean sleepIn(boolean weekday, boolean vacation) {  
  
  return (!weekday || vacation);
}
